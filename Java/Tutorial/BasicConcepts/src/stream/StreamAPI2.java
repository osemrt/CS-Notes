package stream;

/*
 * 
 * List <Integer> values = new ArrayList<>();
 * 
 * values.forEach(System.out::println); // error
 * values.stream().forEach(System.out::println); // it converts the collection to a stream.
 * values.parallelStream().forEach(System.out::println); // it will create multiple threads automatically. if four cores available, it will create four threads. 
 * 
 * The purpose of optional is that what happens if you do not any value.
 * 
 */

public class StreamAPI2 {

}
